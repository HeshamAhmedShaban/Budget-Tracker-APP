import { ValidationFormsDirective } from './validation-forms.directive';

describe('ValidationFormsDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationFormsDirective();
    expect(directive).toBeTruthy();
  });
});
